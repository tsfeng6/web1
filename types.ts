export interface BlobConfig {
  id: string;
  color: string;
  top: string;
  left: string;
  width: string;
  height: string;
  animationDuration: string;
}

export interface IconProps {
  className?: string;
  strokeWidth?: number;
}